@model AdminX.ViewModels.HomeVM
@{
    ViewData["Title"] = "Admin-X Home Page";
}

<div class="container">
    <div class="row mb-5 g-5 card"> 
        
        @if(!Model.isLive)
        {
            <h1 class="text-center redtext">NOTE: This is a test system. Work completed here will not be reflected in production.</h1>
        }
        
        @if (Model.notificationMessage != null && Model.notificationMessage != "")
        {
            <h1 class="text-center redtext">@Model.notificationMessage</h1>
        }

        
            <h2 class="text-center">Welcome, @Model.name!</h2>
            <br />

            <p class="text-center"><img src="~/images/admin-x-logo-2.png" width="500" height="200" /></p>
            <br />

            <h3 class="text-center">The new system for admin team members to manage the database.</h3>
            <br />
            <br />
            <br />
            <div class="row mb-5 g-5">
                <div class="col-md-1"></div>
                <div class="col-md-2">
                    <a type="button" class="btn btn-lg btn-outline-primary" asp-controller="Clinic" asp-action="Index">Contact Outcomes</a>
                </div>        
                <div class="col-md-2">
                    <a type="button" class="btn btn-lg btn-outline-primary" asp-controller="Triage" asp-action="Index">Awaiting Triage</a>
                </div>
                <div class="col-md-2">
                    <a type="button" class="btn btn-lg btn-outline-primary" asp-controller="Triage" asp-action="Index">Cancer ICPs for Review</a>
                </div>
                <div class="col-md-2">
                    <a type="button" class="btn btn-lg btn-outline-primary" asp-controller="Review">Patient Reviews</a>
                </div>        
                <div class="col-md-2">
                    <a type="button" class="btn btn-lg btn-outline-primary" asp-controller="DictatedLetter">Dictated Letters</a>
                </div>
                <div class="col-md-1"></div>
            </div>
        
        <div class="row">
            <div class="col-md-10"></div>
            <div class="col-md-2"><label>Application version: @Model.appVersion</label></div>
        </div>
        <div class="row">
            <div class="col-md-10"></div>
            <div class="col-md-2"><label>Data library version: @Model.dllVersion</label></div>
        </div>
        
    </div>


